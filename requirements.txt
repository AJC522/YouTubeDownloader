# Video Download Manager dependencies
# Install with:  pip install -r requirements.txt

# GUI framework (Qt for Python).
PySide6>=6.6.0

# Backend video/audio downloader. Sites change frequently, so a recent version
# matters; re-run the setup script or `pip install -U yt-dlp` if downloads
# start failing.
yt-dlp>=2025.9.26

# Bundles a static ffmpeg binary so users don't have to install ffmpeg
# themselves. A system-wide ffmpeg on the PATH is used instead when present.
imageio-ffmpeg>=0.5.1
